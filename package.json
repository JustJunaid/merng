{
	"name": "merng-app",
	"private": true,
	"version": "0.1.0",
	"dependencies": {
		"apollo-server": "^2.18.2",
		"graphql": "^15.3.0",
		"mongoose": "^5.10.9"
	},
	"scripts": {
		"start": "node index.js",
		"client": "cd client && yarn && yarn build",
		"build": "yarn client && yarn start"
	}
}
